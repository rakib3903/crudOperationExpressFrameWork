<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Bangladesh</title>

    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 20px;
            background: #f2f4f7;
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        h1 {
            color: #006a4e;
            margin-bottom: 25px;
        }

        .post {
            background: #fff;
            width: 100%;
            max-width: 600px;
            padding: 20px;
            margin-bottom: 20px;
            border-radius: 12px;
            box-shadow: 0px 4px 10px rgba(0,0,0,0.1);
        }

        .user {
            margin: 0;
            font-size: 20px;
            font-weight: bold;
            color: #1a1a1a;
        }

        .content {
            margin-top: 10px;
            color: #444;
            font-size: 17px;
            line-height: 1.5;
        }

        /* Button styling */
        .actions {
            margin-top: 15px;
            display: flex;
            gap: 10px;
            flex-wrap: wrap;
        }

        a, button {
            text-decoration: none;
            background: #006a4e;
            color: white;
            padding: 8px 15px;
            border-radius: 8px;
            border: none;
            cursor: pointer;
            font-size: 15px;
            transition: 0.3s;
        }

        a:hover, button:hover {
            background: #028c65;
        }

        form {
            display: inline;
        }

        /* Create new post link */
        .create-link {
            display: inline-block;
            background: #004b32;
            margin-top: 30px;
        }

        .create-link:hover {
            background: #027a58;
        }

        /* Responsive */
        @media (max-width: 480px) {
            .post {
                padding: 15px;
            }

            .content {
                font-size: 15px;
            }

            a, button {
                padding: 7px 12px;
                font-size: 14px;
            }
        }
    </style>

</head>

<body>
    <h1>Welcome to post management system!</h1>

    <% for(p of data){ %>
        <div class="post"> 
            <h3 class="user"><%= p.userName %></h3>
            <p class="content"><%= p.content %></p>

            <div class="actions">
                <a href="http://localhost:3000/posts/details/<%= p.id %>">Details</a>
                <a href="http://localhost:3000/posts/update/<%= p.id %>">Edit</a>
                <form action="/posts/delete/<%= p.id %>?_method=DELETE" method="post">
                    <button type="submit">Delete</button>
                </form>
            </div>
        </div>
    <% } %>

    <a class="create-link" href="http://localhost:3000/posts/new">Create a New Post</a>

    <script src="/js/index.js"></script>
</body>
</html>
